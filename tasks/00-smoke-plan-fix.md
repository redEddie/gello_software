docs: 리팩토링 계획서의 사실 오류 4곳 수정

docs/collect-workspace-refactor-plan.md 만 고치세요. 다른 파일은 건드리지 마세요.

1. 불변식 4 (23~31줄 근처)와 Phase 3 방식 설명(103줄 근처)의
   "UI 메서드는 상태를 읽기만 한다"는 지킬 수 없는 규칙입니다. 이벤트
   핸들러는 본질적으로 상태를 씁니다. 다음 뜻으로 바꾸세요:
   "상태 변경은 모델의 메서드를 통해서만 한다. UI 코드가 모델의 필드에
   직접 대입하지 않는다."

2. 깨진 글자 3곳을 고치세요:
   - 31줄 "칩 인자 camera node"  -> "카메라 노드"
   - 97줄 "칩 인서 camera / 노드 상태" -> "카메라 / 노드 상태"
   - 126줄 "칩 인에 camera node, 포인트클우드" -> "카메라 노드, 포인트 클라우드"

3. Phase 4 파일 목록(125줄 근처)의 teleop.py 설명에서 "180도 자동 해제"를
   지우세요. 그 기능은 구현된 적이 없습니다 -- 두 번 시도했다 되돌렸고
   GitHub 이슈 #44 로 연기된 상태입니다. "teleop wall, 자세 정렬"만 남기고,
   괄호로 "(#44 케이블 풀기는 아직 없음)"을 덧붙이세요.

4. Phase 3 "대상 상태" 목록(93~97줄)에 상태가 아니라 메서드인 것이 섞여
   있습니다: repo_id_for, _check_repo, _apply_session_config. 세 개를 목록에서
   빼세요. 지우기 전에 apps/collect_workspace.py 에서 grep 해 정말 메서드인지
   (def 로 정의되는지) 확인하고, 메서드가 맞으면 빼세요.

마지막으로 Phase 3 단기계획에 커밋 단위를 명시하세요: 상태 그룹 하나당
커밋 하나로 나눈다 (세션 / 저장소 / 카메라·노드 / UI 상태). 한 커밋에
229개 메서드를 다 손대면 무엇이 무엇을 깨뜨렸는지 추적할 수 없습니다.

코드는 한 줄도 바꾸지 마세요. 끝나면 무엇을 고쳤는지 항목별로 한 줄씩
보고하세요.
